# MiniCal API Server

Серверная часть MiniCal. На данный момент здесь только smoke-сервер (`GET /health`, порт 3001).

Прикладной backend появится в отдельной задаче после `task-003`.
Подробности реализации — в [`backend-agent.md`](.opencode/agents/backend-agent.md).
